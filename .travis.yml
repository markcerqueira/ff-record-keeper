language: 
    - python

python:
    - "2.6"
    - "2.7"
    - "3.2"
    - "3.3"
    - "3.4"
    - "nightly"
  
before_install:
    pip install --upgrade setuptools
    
# install dependencies
install:
    pip install mitmproxy

# command to run tests
script:
    python Test.py