language: 
    - python

python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
    
# install dependencies
install: "curl https://bootstrap.pypa.io/ez_setup.py -o - | python && pip install lxml && pip install mitmproxy"

# command to run tests
script: Test.py